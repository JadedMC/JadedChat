# The official name of the channel. Used in /channel and by other plugins.
name: "STAFF"

# The display name of the channel. Can use color codes.
displayName: "<red>STAFF</red>"

# Aliases for the channel name. Used when switching channels.
aliases:
  - STAFFCHAT
  - SC
  - S

# The permission node required to use the channel.
# Should be empty for the default channel.
permission: "channel.staff"

settings:
  # Whether this is the default channel being used.
  # Only set this to true in one channel.
  default: false

  # Whether the channel should broadcast messages to bungeecord.
  # For this to work the receiving servers must have the same channel.
  bungeecord: true

  # Set if messages sent in this channel should be broadcasted through DiscordSRV.
  # Requires Hooks.DiscordSRV to be enabled in config.yml
  DiscordSRV: false

  # Sets the range (in blocks) that chat messages should travel.
  # Any players outside this distance wont see messages, even if they have permission.
  # set to -1 to disable.
  range: -1

# The available formats in the channel.
formats:
  owner:
    # Format settings.
    settings:
      # Whether the format can use all tags
      all: true

    # The various segments of the message.
    # They are processed in order.
    # You can create/remove as many as you'd like.
    segments:
      channel: "<bold><dark_red>(<red>Staff<dark_red>) "
      prefix: "<bold><red>Owner "
      name: "<hover:show_text:\"<green>Server: <white><server>\"><white>%player_name%</hover>"
      suffix: "<dark_gray> \xbb "
      message: "<red><message>"
  admin:
    # Format settings.
    settings:
      # Whether the format can use all tags
      all: true

    # The various segments of the message.
    # They are processed in order.
    # You can create/remove as many as you'd like.
    segments:
      channel: "<bold><dark_red>(<red>Staff<dark_red>) "
      prefix: "<bold><red>Admin "
      name: "<hover:show_text:\"<green>Server: <white><server>\"><white>%player_name%</hover>"
      suffix: "<dark_gray> \xbb "
      message: "<red><message>"
  mod:
    # Format settings.
    settings:
      # Whether the format can use all tags
      all: true

    # The various segments of the message.
    # They are processed in order.
    # You can create/remove as many as you'd like.
    segments:
      channel: "<bold><dark_red>(<red>Staff<dark_red>) "
      prefix: "<bold><red>Mod "
      name: "<hover:show_text:\"<green>Server: <white><server>\"><white>%player_name%</hover>"
      suffix: "<dark_gray> \xbb "
      message: "<red><message>"
  trial:
    # Format settings.
    settings:
      # Whether the format can use all tags
      all: true

    # The various segments of the message.
    # They are processed in order.
    # You can create/remove as many as you'd like.
    segments:
      channel: "<bold><dark_red>(<red>Staff<dark_red>) "
      prefix: "<bold><red>Trial "
      name: "<hover:show_text:\"<green>Server: <white><server>\"><white>%player_name%</hover>"
      suffix: "<dark_gray> \xbb "
      message: "<red><message>"
  builder:
    # Format settings.
    settings:
      # Whether the format can use all tags
      all: true

    # The various segments of the message.
    # They are processed in order.
    # You can create/remove as many as you'd like.
    segments:
      channel: "<bold><dark_red>(<red>Staff<dark_red>) "
      prefix: "<bold><red>Builder "
      name: "<hover:show_text:\"<green>Server: <white><server>\"><white>%player_name%</hover>"
      suffix: "<dark_gray> \xbb "
      message: "<red><message>"
  developer:
    # Format settings.
    settings:
      # Whether the format can use all tags
      all: true

    # The various segments of the message.
    # They are processed in order.
    # You can create/remove as many as you'd like.
    segments:
      channel: "<bold><dark_red>(<red>Staff<dark_red>) "
      prefix: "<bold><red>Developer "
      name: "<hover:show_text:\"<green>Server: <white><server>\"><white>%player_name%</hover>"
      suffix: "<dark_gray> \xbb "
      message: "<red><message>"
  default:
    # Format settings.
    settings:
      # Whether the format can use all tags
      all: true

    # The various segments of the message.
    # They are processed in order.
    # You can create/remove as many as you'd like.
    segments:
      prefix: "<bold><dark_red>(<red>Staff<dark_red>) "
      name: "<hover:show_text:\"<green>Server: <white><server>\"><white>%player_name%</hover>"
      suffix: "<dark_gray> \xbb "
      message: "<red><message>"